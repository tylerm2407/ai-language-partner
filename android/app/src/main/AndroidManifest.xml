<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <uses-permission android:name="android.permission.INTERNET"/>
  <!-- Live voice tutor (react-native-webrtc). Only these two are missing:
       everything else WebRTC wants (INTERNET, RECORD_AUDIO,
       MODIFY_AUDIO_SETTINGS, SYSTEM_ALERT_WINDOW) is already declared above.

       Added BY HAND rather than via @config-plugins/react-native-webrtc,
       for two reasons. That plugin only takes effect through `expo prebuild`,
       which regenerates this file and would delete the MusicService block
       below — killing Android background news playback. And it injects
       android.permission.CAMERA, which drags in Play Console permission
       declarations for a feature that captures no video.

       Skipping the plugin leaks nothing: the library's own manifest declares
       no permissions at all, only a MediaProjectionService we never start.

       NOT added: BLUETOOTH. The plugin adds the legacy one (maxSdkVersion 30);
       if Bluetooth headset routing is wanted later the modern spelling is
       BLUETOOTH_CONNECT. -->
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
  <uses-permission android:name="android.permission.WAKE_LOCK"/>
  <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
  <uses-permission android:name="android.permission.RECORD_AUDIO"/>
  <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
  <uses-permission android:name="android.permission.VIBRATE"/>
  <!-- Background news-article playback (react-native-track-player).
       iOS has declared UIBackgroundModes:[audio] since launch, but Android had
       no foreground service at all, so playback died the moment the app left
       the foreground on Android 14+. FOREGROUND_SERVICE_MEDIA_PLAYBACK is the
       typed permission Android 14 requires alongside the generic one. -->
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
  <queries>
    <intent>
      <action android:name="android.intent.action.VIEW"/>
      <category android:name="android.intent.category.BROWSABLE"/>
      <data android:scheme="https"/>
    </intent>
  </queries>
  <application android:name=".MainApplication" android:label="@string/app_name" android:icon="@mipmap/ic_launcher" android:roundIcon="@mipmap/ic_launcher_round" android:allowBackup="true" android:theme="@style/AppTheme" android:supportsRtl="true" android:enableOnBackInvokedCallback="false">
    <meta-data android:name="com.google.firebase.messaging.default_notification_color" android:resource="@color/notification_icon_color"/>
    <meta-data android:name="com.google.firebase.messaging.default_notification_icon" android:resource="@drawable/notification_icon"/>
    <meta-data android:name="expo.modules.notifications.default_notification_color" android:resource="@color/notification_icon_color"/>
    <meta-data android:name="expo.modules.notifications.default_notification_icon" android:resource="@drawable/notification_icon"/>
    <meta-data android:name="expo.modules.updates.ENABLED" android:value="false"/>
    <meta-data android:name="expo.modules.updates.EXPO_UPDATES_CHECK_ON_LAUNCH" android:value="ALWAYS"/>
    <meta-data android:name="expo.modules.updates.EXPO_UPDATES_LAUNCH_WAIT_MS" android:value="0"/>
    <activity android:name=".MainActivity" android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|uiMode" android:launchMode="singleTask" android:windowSoftInputMode="adjustResize" android:theme="@style/Theme.App.SplashScreen" android:exported="true" android:screenOrientation="portrait">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
      <intent-filter>
        <action android:name="android.intent.action.VIEW"/>
        <category android:name="android.intent.category.DEFAULT"/>
        <category android:name="android.intent.category.BROWSABLE"/>
        <data android:scheme="fluenci"/>
        <data android:scheme="exp+fluenci"/>
      </intent-filter>
    </activity>
    <!-- react-native-track-player's playback service. Without this declaration
         Android has nowhere to host playback once the activity is backgrounded,
         which is why background listening was iOS-only before now. The
         foregroundServiceType must match the typed permission above. -->
    <service
      android:name="com.doublesymmetry.trackplayer.service.MusicService"
      android:foregroundServiceType="mediaPlayback"
      android:exported="false">
      <intent-filter>
        <action android:name="androidx.media3.session.MediaSessionService"/>
      </intent-filter>
    </service>
  </application>
</manifest>